package gui;

import java.awt.BorderLayout;
import java.awt.Dimension;
import java.awt.GridLayout;
import java.util.Observer;

import javax.swing.JFrame;
import javax.swing.JMenu;
import javax.swing.JMenuBar;
import javax.swing.JMenuItem;
import javax.swing.JPanel;
import javax.swing.JTable;
import javax.swing.border.LineBorder;
import javax.swing.table.AbstractTableModel;
import javax.swing.table.DefaultTableModel;
import javax.swing.table.TableModel;

import domein.LeertrajectController;
import domein.tableModel.LeertrajectTableModel;
import domein.tableModel.SoortTableModel;

public class MainFrame extends JFrame {
	
	/**
	 * 
	 */
	private static final long serialVersionUID = 1L;
	JMenuBar jMenuBar1;
	LeertrajectController controller;
	public MainFrame(LeertrajectController controller ){
		super();
		this.controller = controller;
		initGUI();	
	}

	private void initGUI() {
		try{
			getContentPane().setLayout(new GridLayout(1,2));
		    this.setSize(new Dimension(700,600));
		    
		    // Menu toevoegen
		    // Later tree aanmaken hiervoor
		    jMenuBar1 = new JMenuBar();
			setJMenuBar(jMenuBar1);
			
			{
				JMenu trajectMenu = new JMenu();
				jMenuBar1.add(trajectMenu);
				trajectMenu.setText("Traject");
				{
					JMenuItem trajectAanmaken = new JMenuItem();
					trajectMenu.add(trajectAanmaken);
					trajectAanmaken.setText("Aanmaken");
				}
				{
					JMenuItem trajectVerwijderen = new JMenuItem();
					trajectMenu.add(trajectVerwijderen);
					trajectVerwijderen.setText("Verwijderen");
				}
			}
			{
				JMenu onderdeelMenu = new JMenu();
				jMenuBar1.add(onderdeelMenu);
				onderdeelMenu.setText("Onderdelen");
				{
					JMenu onderdeelToevoegen = new JMenu();
					onderdeelMenu.add(onderdeelToevoegen);
					onderdeelToevoegen.setText("Toevoegen");
					{
						JMenuItem nieuwOnderdeelToevoegen = new JMenuItem();
						onderdeelToevoegen.add(nieuwOnderdeelToevoegen);
						nieuwOnderdeelToevoegen.setText("Nieuw");
					}
					{
						JMenuItem bestaandOnderdeelToevoegen = new JMenuItem();
						onderdeelToevoegen.add(bestaandOnderdeelToevoegen);
						bestaandOnderdeelToevoegen.setText("Bestaand");
					}
				}
				{
					JMenuItem onderdeelVerwijderen = new JMenuItem();
					onderdeelMenu.add(onderdeelVerwijderen);
					onderdeelVerwijderen.setText("Verwijderen");
				}
			}			
			{
				JPanel trajectLijstPanel = new OverzichtPanel(controller.geefTableModel(SoortTableModel.LEERTRAJECT));
				getContentPane().add(trajectLijstPanel);
				
				JPanel trajectDetailsPanel = new TrajectDetailsPanel(controller.geefTableModel(SoortTableModel.ONDERDEEL));
				getContentPane().add(trajectDetailsPanel);
				

				{
				}
			}
			
		}
		catch (Exception e) {
		    //add your error handling code here
			e.printStackTrace();
		}
		
	}


}
